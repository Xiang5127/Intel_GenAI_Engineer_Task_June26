# Intel Local Video AI MVP - backend dependencies (Phase 0 placeholder).
# Pin versions as each phase introduces real dependencies. Keep all inference LOCAL.

# --- Phase 2: gRPC server + proto codegen ---
grpcio==1.81.0
grpcio-tools==1.81.0
protobuf==6.33.6

# --- Local MCP server framework (official Python SDK) ---
mcp==1.27.2

# --- Phase 3: video processing ---
opencv-python==4.12.0.88
imageio-ffmpeg==0.6.0       # bundles ffmpeg binary (system ffmpeg not required)
# moviepy                   # optional alternative; not currently used

# --- Phase 4: local transcription ---
faster-whisper==1.2.1       # local Whisper (CTranslate2); pulls 'av' for audio I/O

# --- Phase 5: vision / OCR ---
openvino==2026.2.0          # OpenVINO vision runtime (preferred); OpenCV fallback
# numpy is pulled in by opencv-python
# OCR: set VISION_DET_MODEL to an OpenVINO IR .xml to enable detection;
# OCR backend optional (pytesseract/easyocr) can be added later.

# --- Phase 6: report generation ---
reportlab==4.5.1            # PDF generation (no model, rule-based summary)
python-pptx==1.0.2          # PPTX generation

# --- Phase 7: planner plan schema validation ---
pydantic==2.13.4            # plan schema models + validation
# Local LLM planner uses Ollama via stdlib urllib (no pip dependency).
# One-time setup (then fully offline):
#   1) install Ollama: https://ollama.com/download
#   2) ollama pull qwen2.5:3b
# Env overrides: OLLAMA_HOST, OLLAMA_MODEL, OLLAMA_TIMEOUT, PLANNER_BACKEND=heuristic|ollama
# Analysis overrides: ANALYSIS_BACKEND=ollama|rule_based, ANALYSIS_MODEL, ANALYSIS_TIMEOUT
# If Ollama is not running, the planner falls back to the offline heuristic stub.

# NOTE: No cloud SDKs. No LangChain/LangGraph. No vector DB.
